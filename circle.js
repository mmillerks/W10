//PRACTICE MODULE #2 (need to finish)
const area = (rad) => {
    return  Math.PI * (rad * rad)
}

const circumference = () => {
    return Math.PI * rad * 2
}


module.exports = {
    area,
    circumference
}